

void executeur(int tab[size][size]) {
    int r;
  printf("quelle est la ligne de la premiere case");
  
  int temp = tab[i1][j1];
    tab[i1][j1] = tab[i2][j2];
    tab[i2][j2] = temp;
  r= premier_vérificateur(int tab);
  while(r=0)
}
